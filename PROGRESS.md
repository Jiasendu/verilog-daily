# 学习进度记录

> 这个文件用于记录每天的学习内容，便于下次接续。

## 📅 总览

| 日期       | Day    | 主题                              | 状态     |
|------------|--------|-----------------------------------|----------|
| 2026-05-19 | Day 01 | 模块基础与 assign 连续赋值        | ✅ 完成  |
| 2026-05-19 | Day 02 | 数据类型 (wire/reg) 与 always 组合逻辑 | ✅ 完成 |
| 2026-05-21 | Day 03 | 运算符进阶 + 位运算应用           | ✅ 完成  |
| 2026-05-22 | Day 04 | 时序逻辑初步（posedge clk）       | ✅ 完成  |
| 2026-05-22 | Day 05 | 基础语法收尾（优先级编码器、casez）  | ✅ 完成  |
| —          | Day 06 | 组合逻辑：译码器（Decoder）          | ⏳ 待开始|

---

## Day 01 - 2026-05-19 ✅

**主题**：Verilog 模块基础、数据类型、assign 连续赋值

**核心知识点**：
- Verilog 描述硬件 vs C/C++ 描述指令
- 模块结构、端口方向、位宽
- 数字字面量格式
- wire 与 assign
- 按位 / 缩位运算符

**练习题完成情况**：4 题全对

---

## Day 02 - 2026-05-19 ✅

**主题**：数据类型（wire/reg）、always 组合逻辑、case 语句、有符号数

**练习题完成情况**：3/4（错在三元方向、漏 default、位宽越界）

---

## Day 03 - 2026-05-21 ✅

**主题**：移位运算、拼接/复制、按位 vs 逻辑 vs 缩位、比较运算

**核心知识点**：
- 四种移位 `<< >> <<< >>>`，算术右移需操作数 `signed`
- 拼接 `{a,b}` 与复制 `{N{x}}`（符号扩展利器）
- 三种"与"：`&`（按位）、`&&`（逻辑判真假）、`&a`（缩位全 1）
- 比较运算符返回 1 位

**练习题完成情况**：
- ⚠️ 练习 1：用了 `<<<` 而非约定的 `<<`（结果对，不规范）
- ❌ 练习 2：8→16 符号扩展，错写成零扩展，正确：`{ {8{data[7]}}, data }`
- ✅ 练习 3：三种"与"运算符辨析全对，`|(a&b)` 写法清晰
- ✅ 练习 4：32-bit 字节序反转，一行拼接搞定

**反思**：
- 三种"与"的辨析过关，缩位运算符思维成型
- 持续问题：动笔前不验证输入输出（Day 02 错 3 题→Day 03 错 1 题，在改善）

---

## Day 04 - 2026-05-22 ✅

**主题**：时序逻辑初步（`always @(posedge clk)`、D 触发器、非阻塞赋值、异步复位）

**核心知识点**：
- `always @(posedge clk or negedge rst_n)` 是时序逻辑的固定模板
- 铁律：时序逻辑用 `<=`（非阻塞），组合逻辑用 `=`（阻塞）
- 非阻塞赋值保证移位链"逐拍流动"，阻塞赋值会导致数据直通
- 同步复位 vs 异步复位（敏感列表的区别）
- `reg` / `wire` 只能在 `module` 层声明，不能在 `always` 块内声明
- `wire` 类型端口要用 `assign` 驱动，不能在 `always` 内赋值

**练习题完成情况**：
- ✅ 练习 1：D 触发器，模板掌握扎实
- ✅ 练习 2：4-bit 使能寄存器，逻辑正确
- ❌ 练习 3：`reg` 在 `always` 内声明 + `wire` 在 `always` 内赋值，两个语法错误
- ❌ 练习 4：同上；脉冲逻辑误用寄存器，正确用 `assign pulse = sig & ~sig_d` 一行解决

**反思**：
- 思路是对的（知道需要中间寄存器、知道要比较前后值）
- 核心问题：把"声明"和"赋值"混写在 `always` 块内
- 记住规则：`always` 块内**只写赋值逻辑**，声明永远在 `module` 顶部

---

## Day 05 - 2026-05-22 ✅

**主题**：基础语法收尾（`if-else` 优先级链、`casez` 无关位、优先级编码器）

**核心知识点**：
- `if-else if` 链综合为优先级树，越靠前优先级越高
- `casez` 用 `?` 表示无关位，匹配顺序即优先级
- 避免 `casex`（将 `x` 当无关位，会掩盖仿真 Bug）
- `!`（逻辑非）vs `~`（按位取反）：判断非零用 `!` 或 `|data`
- 多语句分支必须用 `begin...end`（`if` 和 `case` 都适用）

**练习题完成情况**：
- ✅ 练习 1：4-to-2 优先级编码器（if-else），笔误 `grand`→`grant` 外全对
- ✅ 练习 2：casez 改写，第二版完全正确
- ✅ 练习 3：独热码检测器，`!` vs `~` 区分到位
- ✅ 练习 4：8-to-3 优先级编码器，第三版完全正确

**反思**：
- `begin...end` 遗漏问题在本次重复出现 3 次，需形成"写完条件立刻写 begin...end"的习惯
- `!` vs `~` 已掌握，`|data` 比裸 `data` 更明确

---

## Day 06 - 待开始

**计划方向**：组合逻辑阶段 —— 译码器（Decoder）